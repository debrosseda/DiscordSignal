#ifndef PRIVATECONFIG_H
#define PRIVATECONFIG_H

const char * bot_token = "";

static const String guildID= ""; //Monitored Server's Guild ID

static const String ctrlChannel = ""; //Channel ID where reactions are monitored to change bot behavior

const String KNOWN_ROLE = ""; //Role ID for special mention effects

static const uint8_t USER_OPTIONS = 2; //Number of aliases per known user ID

static const String known_IDs[]={
  "",
  "",
  "",

  "",
  "",
  ""
};



static const int userHues[]={
  145,
  96,
  15
};

const char * portalSSID = ""; //String for SSID used for provisioning with WIFImanager


#endif //PRIVATECONFIG_H
